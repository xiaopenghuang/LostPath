# LostPath 0.3.1

## 界面改版

仪表盘、侧栏、软件磁贴、占用大户列表重做：卡片有了层次与阴影，当前页在侧栏用蓝色
高亮加左侧竖条标出，磁盘条分段可悬停看百分比，没有真实图标的软件按名字生成配色的
首字母磁贴。

## 顺带把对比度问题清了一遍

改版后逐项实测，发现一批**构建检查完全放行**的问题 —— `tsc` 和 `vite build` 对颜色
对比度一律不报错。修掉的（括号内是 WCAG AA 的 4.5:1 门槛下的实测值）：

- 侧栏当前页在浅色主题下只有 **3.58**，深色下旧版是 **3.75**（白字压蓝底）
- 软件首字母磁贴的颜色随软件名变化，穷举 360 种色相：深色 **160 种不过线**
  （最差 2.62），浅色 **全部不过线**（最差 1.12，几乎看不见）
- 悬停在当前页上会把高亮刷掉，看着像点丢了
- 「可清理」「可再生缓存」等分类标签在浅色下 **2.76 ~ 3.37**，而用户正是靠这些
  标签判断什么能删
- 空列表说明文字、链接按钮、幽灵按钮 **3.27 ~ 4.48**

现在两个主题 × 六个页面实测零失败。深色文字对比度从 13.70 提到 **14.99**，
「可清理」标签从 3.37 提到 **5.44**。

## 一个误导性的状态标记

仪表盘标题旁原先有个「实时就绪」徽标，它无条件显示 —— 右边紧挨着的却是「上次扫描
<某天>」。两者可以同时说「就绪」和一个几天前的时间。已删除，扫描时间本身就是状态。

## 内部：对比度现在有测试守着

新增 15 条测试，从样式文件里重新计算每种颜色的对比度、穷举全部 360 种色相验磁贴、
并检查每个颜色是否带着实测标注。用九个真实缺陷做过验证，九个都能让测试变红。

加这个的直接原因：`--tx3` 的注释里写着 4.50 看着达标，真值是 **4.4999**；而
「某个标签浅色只有 3.34」这句话在两个文件的注释里都记着，却一直没修。**数字记在
注释里不会有人校验它。**

## 安装

下载下方的 `LostPath.Setup.0.3.1.exe` 运行。目标机器不需要 Python、conda 或 Node。
安装程序会覆盖旧版本，`%LOCALAPPDATA%\LostPath` 下的扫描快照与配置不受影响。

要求 Windows 10/11 x64。
