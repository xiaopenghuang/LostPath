"""行动力（M4）：把归因结论变成可执行、可回滚的操作。

分层是刻意的——"决定做什么"与"动手做"必须分开，才能在动手之前把决定看清楚：

    planner.py   出计划。**全程只读**，产出"做什么/动哪些文件/腾出多少/有什么拦阻"
    redirect.py  把知识库的重定向提示翻译成可执行机制并分级（只有单个环境变量
                 那一级允许自动执行）
    executor.py  按计划动手，每步写回滚记录（尚未实现）

红线（DESIGN.md §1）：默认只读 + dry-run；写操作必须有回滚记录；优先迁移而非删除。
"""
from .planner import plan_all, plan_for

__all__ = ["plan_all", "plan_for"]
